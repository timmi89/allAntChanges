---
kind: Service
apiVersion: v1
metadata:
  name: memcached-redundant
  labels:
    name: memcached-redundant
    service: memcached
spec:
  ports:
  - name: memcached
    port: 11211
    targetPort: memcached
    protocol: TCP
  selector:
    name: memcached-redundant
    service: memcached
  type: ClusterIP
